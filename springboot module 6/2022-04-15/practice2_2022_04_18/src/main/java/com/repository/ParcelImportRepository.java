package com.repository;

import com.entity.ParcelImport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParcelImportRepository extends JpaRepository<ParcelImport, Long> {
}
